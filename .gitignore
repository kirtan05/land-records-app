# ================= Android / Gradle =================
apps/android/local.properties
**/build/
.gradle/
*.iml
.idea/
captures/
*.apk
*.aab
*.keystore
*.jks
apps/android/app/schemas/

# ================= Node =================
node_modules/
npm-debug.log*

# ================= Python =================
.venv/
__pycache__/
*.pyc

# ============ Chrome automation profiles (large, local only) ============
.chrome-profile/
.chrome-profile-*/

# ============ SECRETS — WhatsApp/Baileys session creds + personal exports ============
packages/whatsapp/auth/
packages/whatsapp/auth-fam/
packages/whatsapp/auth*/
packages/whatsapp/export/
**/creds.json
*.env
.env*

# ============ Generated / bulky data (kept locally, not in VCS) ============
output/
dist/
*.zip

# ============ Unrelated / personal files that must never land here ============
# (the historical ones now live in ../irmsc-personal-archive/)
*.pptx
Invoice-*.pdf
Receipt-*.pdf
.~lock.*#
*.kate-swp
*.kate-swp.*

# ============ Scratch captures / recon dumps ============
Screenshot_*.png
prefill.png
surveynos.jpg
current-page.html
recon-page.html
recon-survey-options.json
detail-*.pdf
detail-*.png
detail-*.html
test-order*.pdf

# ===== Bundled data seed (125 MB of personal land records — NEVER commit) =====
# Regenerable via scratchpad/stage_seed.py from a device snapshot; ships in the APK from
# the working tree, so it never needs to be in git (and must not be, especially publicly).
apps/android/app/src/main/assets/seed/

# ================= IDE =================
.settings/
.project
.classpath
.vscode/

# ================= OS =================
.DS_Store

packages/maps/out/pdf-cache/

# ===== Jantri (ASR-2011) bulk data: ~1.3 GB, fully regenerable =====
# Rebuild with: tools/jantri/fetch.sh && tools/jantri/parse.py && tools/jantri/build_db.py
data/jantri/pdf/
data/jantri/out/
data/jantri/*.sqlite
data/jantri/app/
# villages.csv (1.4 MB) IS tracked -- it is the cross-database join key list.
backups/

# ejamin generated catalogues (15 MB, regenerable via scrape-catalog.mjs)
packages/maps/out/
packages/anyror/anyrorintegrated.html

# ============ Captcha training data + models (large, local, regenerable) ============
packages/captcha/samples/
packages/captcha/kaggle/*
!packages/captcha/kaggle/*.md
*.weights
*.onnx
apps/android/app/src/androidTest/assets/captcha_samples/

# ============ WhatsApp: personal group/contact data (never commit) ============
packages/whatsapp/groups.json
packages/whatsapp/participants.json
packages/whatsapp/qr*.png

# ============ Local scratch captures ============
packages/anyror/*.png
packages/anyror/captcha-ans.txt
packages/anyror/deed-stop.txt

# ============ Monorepo: per-package build + venv output ============
packages/*/.venv/
packages/*/out/
packages/captcha/*.onnx
packages/captcha/*.pt
packages/captcha/*.weights
apps/android/app/build/
